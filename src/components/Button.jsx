
const Button = ({name}) => {
  return (
    <div className='text-center mt-6 pb-52'>
      <button className="btn rounded-full text-bannerBg">{name}</button>
    </div>
  )
}

export default Button
